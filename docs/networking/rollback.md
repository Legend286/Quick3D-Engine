# Networking — Authoritative + Rollback

> **TODO(net):** Transport (ENet), tick rate, ECS-native `[Replicated]` attribute semantics, rollback snapshot machinery.

See [`engine-spec.md` §12](../../engine-spec.md).
