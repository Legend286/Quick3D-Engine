# Audio — Steam Audio Integration

> **TODO(audio):** HRTF / geometry occlusion / convolution reverb wiring through miniaudio buffer post-process. Fallback path for platforms where Steam Audio is unavailable.

See [`engine-spec.md` §10](../../engine-spec.md).
